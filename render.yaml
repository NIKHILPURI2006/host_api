services:
  - type: web
    name: map-navigator-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: MONGODB_URI
        value: "mongodb+srv://mapnavigator:Nikhil@12@cluster0.xxx.mongodb.net/mapnavigator?retryWrites=true&w=majority"

  - type: web
    name: map-navigator-frontend
    env: static
    plan: free
    buildCommand: echo "Frontend building completed"
    staticPublishPath: ./frontend